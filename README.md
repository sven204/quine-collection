Since assembly quines are difficult to define, I have provided descriptions for the corresponding files.
- _quine1.asm_: An assembler source file that generates itself when assembled with fasm.
- _quine2.asm_: A DOS program that generates its own source code when assembled with fasm.

**Todo:** Write quines for the Command Interpreter, nasm and PowerShell.
